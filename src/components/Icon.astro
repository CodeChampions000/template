---
// Example usage:
// import Icon from "..."
// <Icon id="home" cl="home"/>

const {id, cl } = Astro.props

---

<svg class={cl}>
    <use xlink:href={`./src/assets/images/sprite.svg#${id}`}></use>
</svg> 
